    <div class="span-30p event-time-range">
    <!-- Event Date From -->
      <label>{{i18n.time.from}}</label><br/>
      <input name="{{options.from.name}}"
        class="{{options.from.classes}} {{entityType}}-field"
        type="text"
        value="{{options.from.value}}">
    </div>
    <div class="span-30p">
      <label>{{i18n.time.to}}</label><br/>
      <input name="{{options.to.name}}"
             class="{{options.to.classes}} {{entityType}}-field"
             type="text"
             value="{{options.to.value}}">
    </div>
